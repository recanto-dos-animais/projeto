CRIAÇÃO DE TABELAS:
userQuery = `
  CREATE TABLE IF NOT EXISTS usuarios (
    cod_usuario TEXT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    data_nascimento DATE,
    telefone VARCHAR(15),
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(200) NOT NULL
  );
`
adminQuery = `
  CREATE TABLE IF NOT EXISTS admins (
    cod_admin TEXT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    telefone VARCHAR(15),
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(200) NOT NULL
   );
  `
animalQuery = `
  CREATE TABLE IF NOT EXISTS animais (
    cod_animal TEXT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    especie VARCHAR(50) NOT NULL,
    raca VARCHAR(50),
    idade INTEGER,
    sexo VARCHAR(50),
    peso DECIMAL,
    descricao VARCHAR(200),
    status TEXT
  );
`
volunteerQuery = `
  CREATE TABLE IF NOT EXISTS voluntarios (
    cod_voluntario TEXT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    telefone VARCHAR(15),
    endereco VARCHAR(100),
    data_nascimento DATE,
    data_cadastro DATE,
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(200) NOT NULL
  );
`
adoptionQuery = `
  CREATE TABLE IF NOT EXISTS adocoes (
    cod_adocao TEXT PRIMARY KEY,
    cod_animal TEXT NOT NULL,
    cod_usuario TEXT NOT NULL,
    data_adocao DATE,
    status TEXT,
    FOREIGN KEY (cod_animal) REFERENCES animais(cod_animal) ON DELETE CASCADE,
    FOREIGN KEY (cod_usuario) REFERENCES usuarios(cod_usuario) ON DELETE CASCADE
  );
`
donationQuery = `
  CREATE TABLE IF NOT EXISTS doacoes (
    cod_doacao TEXT PRIMARY KEY,
    cod_usuario TEXT NOT NULL,
    data_doacao DATE,
    valor DECIMAL,
    produto VARCHAR(255),
    FOREIGN KEY (cod_usuario) REFERENCES usuarios(cod_usuario) ON DELETE CASCADE
  );
`

INSERÇÃO DE DADOS:
insertUser = `INSERT INTO usuarios (cod_usuario, nome, data_nascimento, telefone, email, senha) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *`
insertAdmin = `INSERT INTO admins (cod_admin, nome, telefone, email, senha) VALUES ($1, $2, $3, $4, $5)`
insertAnimal = `INSERT INTO animais (cod_animal, nome, especie, raca, idade, sexo, peso, descricao, status) VALUES ($1, $2 ,$3, $4, $5, $6, $7, $8, $9) RETURNING *`
insertVolunteer = `INSERT INTO voluntarios (cod_voluntario, nome, telefone, endereco, data_nascimento, data_cadastro, email, senha) VALUES ($1, $2, $3, $4, $5, $6, $7, $8) RETURNING *`
insertAdoption = `INSERT INTO adocoes (cod_adocao, cod_animal, cod_usuario, status) VALUES ($1, $2, $3, $4, $5) RETURNING *`
insertDonation = `INSERT INTO adocoes (cod_doacao, cod_usuario, data_doacao, valor, produto) VALUES ($1, $2, $3, $4, $5) RETURNING *`

EXCLUSÃO DE DADOS:
deleteUserQuery = `DELETE FROM usuarios WHERE cod_usuario = $1`
deleteAdminQuery = `DELETE FROM admins WHERE cod_admin = $1`
deleteAnimalQuery = `DELETE FROM animais WHERE cod_animal = $1`
deleteVolunteerQuery = `DELETE FROM voluntarios WHERE cod_voluntario = $1`
deleteAdoptionQuery = `DELETE FROM adocoes WHERE cod_adocao = $1`
deleteDonationQuery = `DELETE FROM doacoes WHERE cod_doacao = $1`



